# Config Module

Configuration system for InsightSpike.

## Usage

```python
from insightspike.config import get_config
config = get_config()
```

## Key Functions

| Function | Purpose |
|----------|---------|
| `get_config()` | Get configuration dict |
| `Config` | Configuration class |
